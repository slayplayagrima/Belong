Update the Belong platform UI and flow with the following improvements.

IMPORTANT CONTEXT:
- This is an animal adoption platform (NGO-first for now)
- Maintain existing design system (colors, typography, soft UI)
- Do NOT break routing or existing components
- Focus on improving layout scale, clarity, and role-based flow

--------------------------------------------------
1. HERO PAGE FIX (CRITICAL – CURRENT ISSUE)
--------------------------------------------------

Problem:
- Hero components are too small
- Page feels empty and underutilized
- Navbar is cluttered with unnecessary navigation

Fix:

A. Layout Scaling:
- Increase hero section height to min-h-[85vh]
- Increase heading size significantly (text-5xl → text-7xl or larger)
- Increase spacing between elements (gap-6 → gap-10)
- Increase button size and padding

B. Typography:
- Main heading should dominate visually
- Subtext slightly larger and more readable
- Maintain serif for heading, sans for body

C. Image:
- Increase image container size (at least 40–50% width)
- Add subtle shadow and rounded corners
- Ensure it balances text visually

D. Content Structure:
Hero should contain ONLY:
- Logo (Belong)
- Tagline
- Main heading
- Subtext
- CTA buttons

E. REMOVE GLOBAL NAVBAR ITEMS:
- Remove: Animals, Dashboard, NGO Partners from hero page
- This is a marketing landing page, not app navigation

F. Replace Navbar with:
- Left: Logo (Belong)
- Right:
   - "Login"
   - "Register NGO" (secondary CTA)

--------------------------------------------------
2. HERO CTA STRUCTURE
--------------------------------------------------

Primary CTA:
- "Browse Animals"

Secondary CTA:
- "Register as NGO"

Make buttons larger, rounded, and visually strong.

--------------------------------------------------
3. LOGIN PAGE (DUAL MODE: USER + NGO)
--------------------------------------------------

Create /login page with clean UI.

A. Toggle System:
- Tabs or segmented control:
   - "User Login"
   - "NGO Login"

B. Login Options:
Support BOTH:
1. OTP Login (Phone-based)
2. Password Login

C. Fields:

For OTP:
- Phone number input
- Send OTP button
- OTP input field

For Password:
- Email/Phone
- Password

D. UX:
- Clean centered card layout
- Minimal distractions
- Soft shadows, rounded corners

E. NGO Note:
- Add small helper text:
  "NGOs can log in using their registered email and password or OTP"

--------------------------------------------------
4. NGO REGISTRATION FLOW UPDATE
--------------------------------------------------

Update NGO registration:

- Instead of forcing password, allow:
   - Password OR
   - OTP-based login later

- On registration submit:
   → Redirect to /ngo/profile (NOT verification page)

--------------------------------------------------
5. STRUCTURE PREPARATION (IMPORTANT)
--------------------------------------------------

Prepare app for role-based routing:

- /login
- /ngo/register
- /ngo/profile
- /ngo/dashboard

Do NOT implement backend auth yet
Use mock/local state

--------------------------------------------------
6. DESIGN CONSISTENCY
--------------------------------------------------

- Keep light theme, soft colors
- Maintain spacing consistency
- Ensure no component feels “floating” or undersized
- Use max-width containers (max-w-6xl or similar)

--------------------------------------------------
7. OPTIONAL (IF EASY)
--------------------------------------------------

- Add subtle entrance animation to hero text (fade + slide)
- Add hover states on buttons

--------------------------------------------------

DO NOT:
- Add backend logic
- Change global color theme
- Overcomplicate routing
- Add unnecessary sections to hero

FOCUS:
Make the hero feel premium, full, and intentional.
Make login flexible for both users and NGOs.
Prepare clean NGO-first flow.